<div class="number"><%= number + 1 %></div>
<div class="name"><%=  item.name %></div>
<div class="delete"><%= link_to "", user_item_path(current_user, item), method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %></div>
<div class"time"><%= distance_of_time_in_words(Time.now, item.created_at + 7.days) + ' left' %></div>